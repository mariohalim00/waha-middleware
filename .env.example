# Example environment configuration

# Existing configuration
TEXT_BLAST_TEMPLATE="Hello {{name}}, here's your promo!"
PROMO_CODE=YOUR_PROMO_CODE
BASE_WEBFORM_URL=https://your-webform-url.com
BLASTER_WEBHOOK_URL=https://your-webhook-url.com

# WAHA RELATED
API_KEY=
WAHA_URL=http://localhost:3000
SECRET=idontknowthissecretandwhatitisusedforyet

# APP CONFIG
PORT=8080
APP_ENV=local
PROMO_CODE=TEST_PROMO

# DB CONFIG
DB_HOST=
DB_PORT=
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=
DB_SCHEMA=

# OTHER SERVICES URL
TM_API_URL=http://localhost:8081
BASE_WEBFORM_URL=http://localhost:3000

# NEW: Configure concurrent job processing
# Set how many jobs to process concurrently (default: 5)
# Increase for better performance, but be careful not to overwhelm your services
MAX_CONCURRENT_JOBS=10

# Guidelines for MAX_CONCURRENT_JOBS:
# - Start with 5-10 for testing
# - Monitor your external service limits (WhatsApp API, database connections, etc.)
# - Higher values = faster processing but more resource usage
# - Consider your server's capacity and network bandwidth
